# Copy this file to .streamlit/secrets.toml and fill in real values.
# secrets.toml is gitignored — never commit real credentials.

DATABASE_URL = "postgresql+psycopg://healthtracker:healthtracker@localhost:5432/healthtracker"

# GEMINI_API_KEY = "your-gemini-api-key"   # added for real in phase 04 (voice logging)
